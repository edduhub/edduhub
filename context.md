Done — wrote findings to `/Users/kasyap/Documents/edduhub/context.md`.

Key takeaways:
- Protected route smoke coverage is broad via `demo-ready.spec.ts`.
- Deep workflow coverage is solid for attendance, quizzes, files, fees, forum, exams, and self-service.
- Biggest missing E2E areas are auth UI, assignments, grades, calendar, notifications, settings, parent contact, and most admin/faculty ops pages.
- Stable selectors are mostly headings, tabs, explicit input `id`s, and a few good `aria-label`s; there are no app-level `data-testid`s today.